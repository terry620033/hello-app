name: CI/CD Pipeline

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      packages: write

    steps:
<<<<<<< HEAD
    - name: Checkout code
      uses: actions/checkout@v4.2.2

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v3.4.0
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3.10.0

    - name: Build and push ARM64 image
      uses: docker/build-push-action@v6.16.0
      with:
        context: .
        platforms: linux/arm64
        push: true
        tags: |
          ghcr.io/${{ github.repository }}:latest
          ghcr.io/${{ github.repository }}:${{ github.sha }}
=======
      # Checkout application repository
      - name: Checkout source code
        uses: actions/checkout@v5

      # Login to GitHub Container Registry (GHCR)
      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Setup Docker Buildx
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      # Build and push Docker image
      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}:latest
            ${{ env.REGISTRY }}/${{ github.repository }}:${{ github.sha }}
>>>>>>> d6b7c4f2c0abae826b231288970e094eaf1ae90e

      # Checkout GitOps repository
      - name: Checkout GitOps repository
        uses: actions/checkout@v5
        with:
          repository: terry620033/hello-gitops
          token: ${{ secrets.PAT_TOKEN }}
          persist-credentials: true
          path: hello-gitops
    
      # Debug (optional, helpful if it fails)
      - name: Verify GitOps repository
        run: |
          cd hello-gitops
          git remote -v
          git branch
          ls -l

      # Update deployment image tag
      - name: Update deployment image tag
        run: |
          cd hello-gitops

          sed -i "s|image:.*|image: ${{ env.REGISTRY }}/${{ github.repository }}:${{ github.sha }}|g" deployment.yaml

          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"

          git add deployment.yaml

          if git diff --cached --quiet; then
            echo "No changes detected. Skipping commit."
            exit 0
          fi

          git commit -m "Update image to ${{ github.sha }}"
          git push origin main
